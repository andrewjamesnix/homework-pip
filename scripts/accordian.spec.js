describe('Accordian', () => {
    const component = new AccordianComponent();
    component.init();
    beforeEach(() => {
        component.init();


    });

    // it('should render 8 divs', () => {
    //     expect(component).toBe(8);
    // });
    //
    // it('should render 4 heading elements', () => {
    //     expect(component.headingEls.length).toBe(4);
    // });
    //
    // it('should render 4 content elements', () => {
    //     expect(component.contentEls .length).toBe(4);
    // });
    //
    // it('should render 4 content elements', () => {
    //     component.headingEls[0].click();
    //     console.log(component.contentEls);
    //     expect(component.contentEls).toBe('none');
    // });


});